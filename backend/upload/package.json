{
  "name": "flipcut-upload",
  "version": "1.0.0",
  "description": "FlipCut image upload Lambda",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && cd dist && zip -r ../function.zip . && cd .. && zip -r function.zip node_modules"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
